# Jira Auto-Close Bot

Автоматично затваря Jira тикети, които са в статус "Waiting for Customer" повече от 5 работни дни (без събота и неделя).

## Функционалност

- ✅ Проверява тикети в "Waiting for customer" статус
- ✅ **Филтрира по проект** (например само RT проект)
- ✅ **SLA Breach проверка** - затваря само тикети, при които И ДВЕТЕ SLA-та са breach-нати:
  - Time to first response
  - Time to resolution
- ✅ Изчислява работни дни (без събота и неделя)
- ✅ Автоматично assign-ва тикета на бот акаунта
- ✅ Затваря тикети след 5+ работни дни (конфигурируемо)
- ✅ Добавя коментар с обяснение преди затваряне
- ✅ Автоматично се стартира всеки работен ден (опционално)
- ✅ Robust error handling - не спира при грешки
- ✅ Автоматично създава Bug тикет при критични грешки (с full ADF format)
- ✅ Записва логове в `log/bot_log.txt`

## 🛡️ Error Handling & Robustness

Ботът е проектиран да работи непрекъснато без човешка намеса:

- **Устойчив на грешки:** Ако има проблем с един тикет, прескача го и продължава с останалите
- **Не спира работа:** При грешка показва детайли, но продължава на следващото стартиране
- **Автоматично уведомяване:** При критична грешка създава Bug тикет в Jira за investigation
- **Подробно логване:** Всички операции и грешки се записват в log файл
- **Graceful degradation:** 
  - Ако assign-ването фейлне → продължава със затварянето
  - Ако коментарът фейлне → продължава със затварянето
  - Ако затварянето фейлне на един тикет → продължава със следващия

## Изисквания

- Python 3.7+
- Jira акаунт за бота с права:
  - Browse Projects
  - Assignable User
  - Close Issues
  - Add Comments
  - Transition Issues

## Инсталация

### 1. Инсталирайте Python зависимости

```bash
pip install -r requirements.txt
```

### 2. Създайте конфигурационен файл

Копирайте `config.json.example` към `config.json`:

```bash
copy config.json.example config.json
```

### 3. Конфигурирайте бота

Редактирайте `config.json` и попълнете:

```json
{
  "jira_url": "https://yourcompany.atlassian.net",
  "username": "bot@yourcompany.com",
  "api_token": "YOUR_API_TOKEN_HERE",
  "days_threshold": 5,
  "dry_run": false,
  "project": "RT",
  "error_project": "RT"
}
```

**Параметри:**
- `jira_url`: URL на вашата Jira инстанция
- `username`: Email на бот акаунта
- `api_token`: API токен (генерирайте от: https://id.atlassian.com/manage-profile/security/api-tokens)
- `days_threshold`: Брой работни дни преди автоматично затваряне (по подразбиране: 5)
- `dry_run`: Ако е `true`, само показва тикетите без да ги затваря (за тестване)
- `project`: **(Ново!)** Проект key за филтриране (например "RT"). Ако липсва - търси във всички проекти
- `error_project`: **(Ново!)** Проект за създаване на Bug тикети при грешки (по подразбиране: "RT")

## Употреба

### Ръчно стартиране

```bash
python jira_auto_close.py
```

### Тестов режим (Dry Run)

За да видите кои тикети ще бъдат затворени без да ги затваряте реално:

Променете в `config.json`:
```json
"dry_run": true
```

След това:
```bash
python jira_auto_close.py
```

### Автоматично ежедневно стартиране

#### Windows Task Scheduler

1. Отворете Task Scheduler
2. Създайте нова задача (Create Task)
3. General таб:
   - Name: "Jira Auto-Close Bot"
   - Run whether user is logged on or not
4. Triggers таб:
   - New Trigger
   - Daily, every 1 day
   - Start time: 09:00 (или по ваш избор)
   - Enabled
5. Actions таб:
   - New Action
   - Program/script: `python.exe` (или пълен път до python)
   - Add arguments: `jira_auto_close.py`
   - Start in: `C:\Users\OGS\Desktop\bot` (пътя до бота)
6. Conditions таб:
   - Премахнете "Start the task only if the computer is on AC power"

#### Използвайте включения scheduler script

Алтернативно, използвайте PowerShell скрипта:

```powershell
.\run_daily.ps1
```

За да добавите към Task Scheduler:
```powershell
.\setup_scheduler.ps1
```

## Примерен изход

```
============================================================
Jira Auto-Close Bot - Starting...
Date: 2026-01-20 09:00:00
Threshold: 5 working days
Project: RT
Mode: LIVE
============================================================
Searching for tickets with status: Waiting for customer
  Filtering by project: RT
Ticket RT-123: 7 working days in Waiting for customer
  SLA customfield_10020: ✗ BREACHED
  SLA customfield_10021: ✗ BREACHED
  → SLA Status: BOTH BREACHED ✗ (2/2)
  → Will be closed (SLA breached)
Ticket RT-456: 6 working days in Waiting for customer
  SLA customfield_10020: ✓ Not breached
  SLA customfield_10021: ✗ BREACHED
  → SLA Status: Not both breached ✓ (1/2)
  → Skipping (SLA not breached)

Found 1 ticket(s) to close:
  - RT-123: Customer not responding to email request
    Status changed: 2026-01-10
    Working days: 7

Closing tickets...

Processing RT-123...
  Assigning to bot account...
  ✓ Assigned
  Adding comment...
  ✓ Comment added
  Closing ticket...
✓ Ticket RT-123 closed successfully

============================================================
Summary: Closed 1 out of 1 ticket(s)
============================================================
```

## Сигурност и Логове

⚠️ **ВАЖНО**: Никога не публикувайте `config.json` с реални credentials!

### 🔒 Какво НЕ е в GitHub:
- `config.json` - Конфигурационен файл с API token (използвайте config.json.example)
- `log/bot_log.txt` - Локални логове (автоматично се създават при първо стартиране)
- Всички `.log` файлове

### 📁 Структура на логовете:
- Папката `log/` **съществува** в repository (чрез `.gitkeep`)
- Файлът `bot_log.txt` се **създава автоматично** при първо стартиране на бота
- Логовете остават **локални** и не се качват в GitHub
- Всяко устройство има свои логове

### 🚀 При клониране на проекта:
```bash
git clone https://github.com/radostinaaa/AutoCloseTickets_JiraBot.git
cd AutoCloseTickets_JiraBot
```
1. Копирайте `config.json.example` към `config.json`
2. Попълнете вашите Jira credentials
3. Инсталирайте зависимости: `pip install -r requirements.txt`
4. При първо стартиране логовете ще се създадат автоматично в `log/bot_log.txt`

- `config.json` е добавен в `.gitignore`
- Използвайте само `config.json.example` за споделяне
- API токените трябва да се пазят в тайна

## Troubleshooting

### Проблем: "Could not find close transition"

Вашият Jira workflow може да има различно име на затваряща транзакция. Проверете достъпните транзакции в изхода и актуализирайте кода ако е нужно.

### Проблем: "Authentication failed"

- Проверете дали API токена е валиден
- Уверете се, че username-а е точен email адрес
- Проверете дали Jira URL е правилен

### Проблем: "Permission denied"

Бот акаунта трябва да има следните права:
- Browse Projects
- Assignable User  
- Close Issues
- Add Comments
- Transition Issues

### Проблем: "HTTP 410" грешка

Старите версии на Jira библиотеката използват deprecated API endpoint. Ботът вече използва новия `/rest/api/3/search/jql` endpoint директно.

### Проблем: "SLA fields not found"

Ако ботът не намира SLA полета:
1. Проверете дали в проекта има настроени SLA-та
2. SLA полетата са customfield-ове и могат да имат различни номера в различни Jira инстанции
3. Използвайте **Test 5** от test_scripts.py за да проверите SLA статус на конкретен тикет

## Test Scripts

Ботът идва с набор от тестови скриптове за проверка на функционалността:

```bash
python test_scripts.py
```

**Налични тестове:**

1. **Force Close Specific Ticket** - Затваря конкретен тикет независимо от SLA или дни
2. **Simulate Error & Create Bug Ticket** - Тества Bug ticket създаването
3. **Create Test Ticket in 'Waiting for customer'** - Създава тестов тикет
4. **Test Invalid Configuration Error Handling** - Тества error handling
5. **Check SLA Breach Status** - **(Ново!)** Проверява SLA статус на конкретен тикет
6. **Run ALL Tests** - Пуска всички тестове последователно

## Персонализация

### Промяна на статус

Ако статусът във вашата Jira е различен от "Waiting for customer", можете да го промените в кода:

```python
tickets_to_close = self.find_old_waiting_tickets(
    days_threshold=5, 
    status_name="Вашия Статус"
)
```

### SLA Логика

По подразбиране ботът затваря тикети само ако **И ДВЕТЕ условия** са изпълнени:
1. Тикетът е в "Waiting for customer" повече от 5 работни дни
2. **И двете SLA-та** (Time to first response И Time to resolution) са breach-нати

Ако искате да промените тази логика, модифицирайте `check_sla_breach()` функцията в `jira_auto_close.py`.

### Филтриране по проект

За да ограничите бота само до конкретен проект, добавете в `config.json`:
```json
"project": "RT"
```

За да работи с всички проекти, изтрийте `project` полето или оставете го празно.

## Важни бележки

⚠️ **SLA Проверка**: Ботът затваря тикети САМО ако:
- Тикетът е в "Waiting for customer" повече от 5 дни (конфигурируемо)
- **И** и двете SLA-та са breach-нати

Това гарантира, че не се затварят тикети с активни или непробити SLA-та.

## Лиценз

MIT
